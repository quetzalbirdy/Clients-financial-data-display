/* import { configureStore } from '@reduxjs/toolkit';
import estadoAppSliceReducer from '../features/counter/estadoAppSlice';

export default configureStore({
  reducer: {
    estadoApp: estadoAppSliceReducer,
  },
});
 */